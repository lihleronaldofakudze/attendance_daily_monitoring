name: attendance_daily_monitoring
description: Attendance - Daily Monitoring

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  google_fonts: ^2.1.0
  provider: ^5.0.0
  firebase_core: ^1.3.0
  firebase_auth: ^1.4.1
  animated_splash_screen: ^1.1.0
  pin_code_fields: ^7.3.0
  flutter_spinkit: ^5.0.0
  font_awesome_flutter: ^9.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
     - images/